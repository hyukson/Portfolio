import styled from "styled-components";

export const WeatherAniStyled = styled.div`
  position: absolute;
  width: 100vw;
  height: 100vh;
  pointer-events: none;
  user-select: none;

  canvas {
    pointer-events: none;
    user-select: none;
  }
`